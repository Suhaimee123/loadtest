name: Manual Load Test with k6

on:
  push:
    branches: [ main ] # รันทันทีเมื่อมีการ push
  workflow_dispatch: # ทำให้มีปุ่มกด Run workflow ในหน้าเว็บ GitHub
  schedule:
    - cron: '0 */2 * * *' # รันอัตโนมัติทุกๆ 2ชั่วโมง

jobs:
  k6_load_test:
    name: k6 Load Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run k6 test
        id: k6test
        uses: grafana/k6-action@v0.3.1
        with:
          filename: loadtest.js

      - name: Generate Summary
        if: always()
        run: |
          if [ -f "summary.md" ]; then
            cat summary.md >> $GITHUB_STEP_SUMMARY
          else
            echo "## ❌ Error: summary.md not found" >> $GITHUB_STEP_SUMMARY
            echo "Check k6 console logs for errors." >> $GITHUB_STEP_SUMMARY
          fi